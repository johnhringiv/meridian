<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-feature android:name="android.hardware.type.watch" />

  <!-- hasCode must be false for Watch Face Format: the package is resource-only. -->
  <application
      android:label="@string/watch_face_name"
      android:hasCode="false">

    <meta-data
        android:name="com.google.android.wearable.standalone"
        android:value="true" />
    <!-- Lowest WFF version that covers this face (arcs, conditions, battery tag,
         complications, color configuration are all v1) — maximizes device reach. -->
    <property
        android:name="com.google.wear.watchface.format.version"
        android:value="1" />
  </application>
</manifest>
